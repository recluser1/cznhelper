type Props = {
    characterName: string;
    characterImage: string;
    attribute: string;
    class: string;
}
const Overview = (props: Props) => {
    const { characterName, characterImage, attribute, class: charClass } = props;
    return (
        <></>
    )
}